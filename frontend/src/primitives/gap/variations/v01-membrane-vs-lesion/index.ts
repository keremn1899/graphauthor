import type { GapVariation } from "../../types";
import { Intended } from "./Intended";
import { Oversight } from "./Oversight";

export const v01MembraneVsLesion: GapVariation = {
  id: "v01",
  thesis: "Sealed membrane vs sparse remains",
  note: "Intended holds a full boundary; oversight is mostly void with only a few leftover edge fragments.",
  Intended,
  Oversight,
};
