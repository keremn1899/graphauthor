import type { GapVariation } from "../../types";
import { Intended } from "./Intended";
import { Oversight } from "./Oversight";

export const v04CornerAuthorityVsDissolution: GapVariation = {
  id: "v04",
  thesis: "Intact membrane vs blunt breach",
  note: "Intended is unbroken; oversight keeps three walls but the fourth simply stops — a hole, not a doorway.",
  Intended,
  Oversight,
};
