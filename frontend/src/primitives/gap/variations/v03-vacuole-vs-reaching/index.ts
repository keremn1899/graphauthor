import type { GapVariation } from "../../types";
import { Intended } from "./Intended";
import { Oversight } from "./Oversight";

export const v03VacuoleVsReaching: GapVariation = {
  id: "v03",
  thesis: "Firm edge vs progressive fray",
  note: "Three sides stay decided; the fourth breaks into shortening fragments that fade.",
  Intended,
  Oversight,
};
